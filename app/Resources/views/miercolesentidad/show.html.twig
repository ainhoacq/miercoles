{% extends 'base.html.twig' %}

{% block body %}
<body style="width: 70%; margin: 8%">

    <h1>Ver registro</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ miercolesEntidad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ miercolesEntidad.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ miercolesEntidad.apellido }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ miercolesEntidad.direccion }}</td>
            </tr>
        </tbody>
    </table>
          
            
    <ul>

        <a href="{{ path('miercolesentidad_index') }}" class="btn btn-info">Back to the list</a> <br>

            <br><a href="{{ path('miercolesentidad_edit', { 'id': miercolesEntidad.id }) }}" class="btn btn-warning">Edit</a> <br> <br>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}

    </ul>
            
</body>            
{% endblock %}

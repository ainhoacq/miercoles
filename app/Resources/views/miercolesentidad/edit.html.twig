{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editar registro</h1>
    <body style="width: 70%; margin: 8%">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br><input type="submit" value="Edit" class="btn btn-warning" />
    {{ form_end(edit_form) }}

<br>
    <a href="{{ path('miercolesentidad_index') }}" class="btn btn-info">Back to the list</a> <br><br>

            {{ form_start(delete_form) }}
            <input type="submit" value="Delete" class="btn btn-danger"> <br><br>
            {{ form_end(delete_form) }}

    </body>
{% endblock %}
